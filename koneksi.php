<?php
require './libs/rb.php';

/**
 * uncomment this for database connection
 * fill the params with correct database for your project
 * 
R::setup( 'mysql:host=localhost;dbname=dbname', 'root', '' );

if(!R::testConnection()) die('No DB connection!');
*/
?>
